# KLayout 2D Layout Feature Gaps

Date: 2026-05-18

This compares Glassworks's current 2D layout editor against KLayout's documented layout-editor feature set. It is scoped to 2D layout authoring, viewing, verification, import/export, and layout automation. It does not treat Glassworks's fab workflow, MES, 3D stack, or collaboration modules as KLayout gaps.

Glassworks already has a useful 2D baseline: layer visibility, rendered per-layer fill/line display styles, options-backed fixed hierarchical layer group and used/empty/visible/hidden row filtering, options-backed per-layer hierarchy depth overrides, drawing rectangles/polygons/paths/vias/labels/direct-horizontal-vertical-Manhattan measurements with live filter counts plus active filter/search row-count titles, listed-ruler aggregate summaries, selected-ruler focus/deletion, measurement sort controls, and undoable visible-ruler cleanup, vertex and edge editing, selected-object properties, selected-shape, selected-instance, and current-cell custom key/value property set/remove controls, stored shape/cell/instance key/value metadata, hierarchy/cells/instances/arrays, a small parameterized via-array library macro, GDSII subset import/export including label/TEXT records, orthogonal SREF/AREF rotation/reflection round-tripping, PATH square/custom end-extension import, shape-name/net-ID/stored-shape-and-instance-key-value property round-tripping, and BOX import as geometry, File-menu exchange, and gzip-/single-file-ZIP-aware import, CIF subset import/export including basic hierarchical `DS`/translated-`C` import/export, File-menu exchange, and gzip-/single-file-ZIP-aware import, DXF subset import/export including lines, polylines, text, circles/arcs, basic rotated/mirrored `BLOCK`/`INSERT` hierarchy, File-menu exchange, and gzip-/single-file-ZIP-aware import, DEF subset import/export with flat geometry plus basic orthogonal `COMPONENTS` metadata, File-menu exchange, and gzip-/single-file-ZIP-aware import, basic multi-macro LEF macro-cell subset import/export including File-menu exchange and gzip-/single-file-ZIP-aware import, File-menu app-session JSON plus layout JSON and workspace JSON save/load through validated model serializers with gzip-/single-file-ZIP-aware loading, File-menu JSON, GDS, CIF, DXF, DEF, and LEF import as a child-cell instance, extra top cell, flattened merge, or hierarchy-preserving root merge, selectable JSON/GDS/CIF/DXF/DEF/LEF import layer mapping by ID/name/copy policy with a layer-ID offset for newly imported/copy layers, import-offset controls for JSON/GDS/CIF/DXF/DEF/LEF import-as-cell, flattened merge, and hierarchy-merge placement, recent native session/layout/workspace/GDS/CIF/DXF/DEF/LEF/reference-image/DRC-deck/DRC-report/DRC-database/Calibre-RVE/L2N-database exchange entries, gzip-/single-file-ZIP-aware DRC deck/report/database and Calibre/RVE marker imports, browser-driven replacement for shape names, shape property keys/values, layer names, label/ruler text, cell names, cell property keys/values, instance names, instance property keys/values, and active-report DRC marker review notes/owners/signoffs/tags with document/current-cell/visible-hierarchy scope controls for layout objects, deterministic app-session, named layer-set, layout-view-bookmark, and reference-image JSON exchange with per-image visibility, focus, opacity, order, and removal controls, basic data-driven DRC with importable/exportable custom JSON decks, axis-aligned min-edge-spacing checks, bounds-based named `AND`/`OR`/`NOT` derived layers with derived min/max-width, min/max-area, min-spacing, min-edge-spacing, and forbidden-overlap checks, plus full-layout/current-cell/selected-region runs, an active-report DRC marker browser with bounded session-restored report history, rule-family grouping, rule-path plus user-tagged category directory rows and filter controls, selected-marker info rows, persisted review state/notes/owner/signoff/preset-and-custom key-value-tag metadata, selected-marker RGBA/PNG snapshot export with path/bounds/size/format marker tags, active-report and report-database JSON exchange, Calibre/RVE-style text marker import, and active-marker annotation-layer geometry output, connectivity extraction with deterministic extracted-netlist JSON, simple MOS-like diffusion/poly with overlapping well/body-label bulk inference, isolated labeled-poly resistor-like, and labeled metal-overlap capacitor-like device extraction, SPICE-style `.subckt` export, supported-device SPICE schematic comparison, trace-state JSON exchange, and combined L2N database JSON exchange, explicit route/DRC actions, individual/sibling/immediate-child/descendant view-only cell hide/show controls, and a small named/options-backed layout view bookmark set. The gaps below are things KLayout has that Glassworks does not yet have or only covers in a narrow demo form.

The 2D side panel and inspector also include a searchable technology stack browser for the active built-in technology, covering DBU/grid, active-layer GDS/text mapping, connectivity links, active-layer stack participation, and DRC rule-family counts with `key=value` selectors for technology, DRC, layer, mapping, and connectivity-stack metadata. The active built-in technology can be switched from the Tools menu and drives document layer definitions, grid, connectivity extraction, GDS mapping, netlist metadata, and the built-in DRC deck while preserving imported/custom extra layers. Individual connectivity stack links can also be toggled for trace/net extraction experiments without changing layer visibility or DRC rules.

## High-Value Missing Features

### Rich Layout File Support

- OASIS read/write.
- Fuller LEF beyond Glassworks's current basic multi-macro macro-cell subset, fuller DEF beyond Glassworks's current flat geometry plus basic orthogonal `COMPONENTS` metadata subset, fuller GDSII compatibility beyond Glassworks's current hierarchy/AREF/path-extension/path/label/oriented-reference/property-name-net-shape-and-instance-key-value/BOX-import subset, fuller CIF beyond Glassworks's current basic `DS`/translated-`C` hierarchy import/export subset, fuller DXF beyond Glassworks's current ASCII line/polyline/text/circle/arc and basic rotated/mirrored `BLOCK`/`INSERT` subset, compressed layout loading beyond Glassworks's current gzip- and single-file-ZIP-aware app-session/layout JSON/workspace JSON/GDS/CIF/DXF/DEF/LEF/reference-image load paths, and richer reader options.
- Multi-file loading into separate views or the same panel.
- Drag/drop file loading, URL loading, and richer file dialogs beyond Glassworks's current deterministic File-menu app-session JSON, layout JSON, import-layout-as-cell JSON/GDS/CIF/DXF/DEF/LEF, import-extra-top-cell JSON/GDS/CIF/DXF/DEF/LEF, merge-layout JSON/GDS/CIF/DXF/DEF/LEF, GDS/CIF/DXF/DEF/LEF/reference-image exchange, gzip- and single-file-ZIP-aware app-session/workspace/layout/GDS/CIF/DXF/DEF/LEF/reference-image loading, workspace JSON snapshot paths, reload-most-recent action, and options-backed recent entries for those native exchange paths.
- Importing another layout into the current layout beyond Glassworks's current JSON/GDS/CIF/DXF/DEF/LEF-as-remapped-child-cell instance, extra-top-cell, flattened top-level merge, and hierarchy-preserving root-merge workflows, including selectable instantiate, richer top-cell handling, and deeper cross-format merge policies.
- Richer reader-specific import mapping beyond Glassworks's current selectable JSON/GDS/CIF/DXF/DEF/LEF import layer mapping by ID/name/copy policy, JSON/GDS/CIF/DXF/DEF/LEF layer-ID offset for newly imported/copy layers, and File-menu import-offset placement transform controls.

KLayout source: [Loading A File](https://www.klayout.org/downloads/master/doc-qt4/manual/loading.html), [Import Other Layout Files](https://www.klayout.org/downloads/master/doc-qt5/manual/import_layout.html)

### Hierarchy Viewing And Navigation

- Navigate and manage cells from a richer tree-structured hierarchy browser. Glassworks now has root-cell-aware 2D rendering/picking, numeric min/max hierarchy display depths, parent/child context actions, selected-instance descend, a flat all-cell browser with current/used/unused/library/property-bearing/empty/leaf/branch/parent/child/sibling quick filters, searchable hierarchy-depth and parent/child/sibling/ancestor/descendant relation rows, and name/id/hierarchy-depth/shape-count/instance-count sort controls, and a searchable collapsible hierarchy tree with row-count titles, search-revealed matching descendant paths, and bulk expand/collapse controls for choosing the 2D view top cell, but not KLayout's full hierarchy browser feature set.
- Hide/show cell workflows beyond Glassworks's current view-only individual cell, sibling group, immediate-child group, descendant group, and show-all hidden-cell controls in the 2D hierarchy tree.
- Configure cell lists and browse cells with richer hierarchy controls beyond Glassworks's current flat browser filters, hidden/visible cell filters, leaf/branch/current-parent/current-child/sibling/ancestor/descendant filters, searchable hierarchy-depth and parent/child/sibling/ancestor/descendant relation rows, hierarchy-depth sorting, and searchable collapsible hierarchy tree with bulk expand/collapse controls.

KLayout source: [Choosing A Cell](https://klayout.org/downloads/master/doc-qt5/manual/cell.html), [Choosing A Hierarchy Depth](https://www.klayout.org/downloads/master/doc-qt5/manual/hier.html), [KLayout Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/basic.html)

### Advanced Hierarchy Editing

- Flatten selected instances beyond Glassworks's current undoable document-top, current-cell, and deeper selected-occurrence one-level or deep instance flattening, including batch selections and richer depth policies.
- Flatten whole cells beyond Glassworks's current undoable one-level or deep current-view-cell, descendant-cell batch, and whole-document batch flattening, including multi-selection batch flatten workflows.
- Move selected shapes or instances up in hierarchy beyond Glassworks's current undoable current-cell and deeper selected-occurrence shape/instance move-up actions, including richer multi-parent/cell-variant choices.
- Resolve arrays into editable individual instances beyond Glassworks's current selected document-top, current-cell, deeper selected-occurrence, current-cell batch, descendant-cell batch, and whole-document batch array resolve actions, including partial-array policies and multi-selection batch scopes.
- Create cell variants beyond Glassworks's current shallow document-top, current-cell, deeper selected-occurrence, current-cell child-instance batch, descendant child-instance batch, and document-wide child-instance batch variant actions plus standalone current-cell duplication, including richer occurrence-specific variant policies.
- Delete cells beyond Glassworks's current guarded unused-cell, all-unused-cell batch, shallow current-cell, unshared-subtree deep current-cell, and full-subtree complete current-cell delete actions, including richer multi-cell selection management and deletion policies.
- Adjust cell origins beyond Glassworks's current undoable origin-to-selection, browser-search exact DBU coordinate, grid-derived +/-X/Y origin actions for the current 2D view cell, and descendant leaf-cell origin-to-local-bounds batching, including richer non-leaf or multi-selection batch workflows.

Glassworks has basic current-cell create-cell, place-instance, transform, array metadata, instance deletion, shallow document-top/current-cell/deeper selected-occurrence plus current-cell, descendant, and document-wide child-instance batch variants, standalone current-cell duplication, Edit-menu and hierarchy-context one-level/deep flatten policies for document-top/current-cell/deeper selected-occurrence instance flattening, current-view-cell, descendant-cell batch, and whole-document batch one-level or deep flattening, origin-to-selection, exact browser-search DBU coordinate origins, grid-derived origin nudges for the current view cell, and descendant leaf-cell origin-to-local-bounds batching, current-cell/deeper selected-occurrence shape and instance move-up into parent placements, Edit-menu and hierarchy-context document-top/current-cell/deeper selected-occurrence array resolving plus current-cell, descendant-cell, and whole-document batch array resolving, guarded unused-cell and all-unused-cell batch deletion, Edit-menu and hierarchy-context shallow/deep/complete current-cell deletion that can remove references, prune unshared descendants, or remove a full descendant subtree with external references, but not these richer KLayout hierarchy workflows.

KLayout source: [Advanced Editing Operations](https://www.klayout.org/downloads/master/doc-qt5/manual/editor_advanced.html), [Hierarchical Operations](https://klayout.org/downloads/master/doc-qt5/manual/hier_ops.html), [Resolving Arrays](https://www.klayout.org/downloads/master/doc-qt5/manual/resolve_arrays.html), [Delete A Cell](https://www.klayout.org/downloads/master/doc-qt5/manual/del_cell.html)

### Geometry Operations

- Layer boolean operations beyond Glassworks's current active-layer current-view-cell exact rectangle union merge, selected current-cell rectangle/polygon active-layer OR/AND/NOT/selection-NOT-layer/XOR fragment workflows, and active-layer current-cell layer AND/OR/NOT/XOR against one-or-more clipboard rectangle/polygon operands: arbitrary layer operands, hierarchy-aware output, and CAD-grade polygon boolean merging beyond selected-region or clipboard decomposition.
- Shapewise boolean operations beyond Glassworks's current active-layer-to-clipboard capture plus selected current-cell shape versus one-or-more clipboard-shape rectangle/polygon AND/OR/NOT/XOR workflow, including true multi-selected-shape operands, hierarchy-aware output, and CAD-grade polygon boolean merging beyond selected-shape decomposition.
- Layer sizing and full shapewise sizing beyond Glassworks's current selected-shape and active-layer current-view-cell rectangle/polygon/path/via grow-shrink actions plus X-only/Y-only current-cell rectangle/polygon sizing, including hierarchy-aware sizing, split/merged offset outputs, and richer sizing modes.
- Object alignment tools beyond Glassworks's current selected-shape and selected-instance edge/center alignment against active-layer parent bounds, origin-axis centering, and active-layer current-cell shape alignment against a selected reference shape.
- Corner rounding and smoothing beyond Glassworks's current selected and active-layer current-cell rectangle/polygon corner chamfer and rounded-corner approximation operations, including smoother curve generation, hierarchy-aware output, and richer batch workflows.
- Clip creation from regions beyond Glassworks's current selected-region or clipboard-region current-cell active-layer rectangle-or-polygon clip-cell actions.
- Search and replace beyond Glassworks's current browser-driven replacement for shape names, shape property keys/values, layer names, label/ruler text, cell names, cell property keys/values, instance names, instance property keys/values, and active-report DRC marker review notes/owners/signoffs/tags with document/current-cell/visible-hierarchy scope controls for layout objects, including richer object properties and cross-layout workflows.

Glassworks currently has direct manual editing, current-cell shape control selection, current-cell shape drag/alt-drag duplication and vertex/edge editing, current-cell shape copy/paste/duplicate/delete and rotate/mirror transforms, selected-shape and active-layer current-view-cell rectangle/polygon/path/via grow/shrink operations, X-only/Y-only selected and active-layer current-cell rectangle/polygon sizing, selected and active-layer current-cell rectangle/polygon corner chamfering and rounded-corner approximation, selected current-cell shape and selected-instance edge/center alignment against active-layer parent bounds plus origin-axis centering, active-layer current-cell shape alignment against a selected reference shape, active-layer current-view-cell exact rectangle union merging, selected current-cell rectangle/polygon active-layer OR/AND/NOT/XOR plus selection-NOT-layer clipping through convex selected-region decomposition, active-layer current-cell clipboard capture, active-layer current-cell layer AND/OR/NOT/XOR against one-or-more clipboard rectangle/polygon operands, selected current-cell shape versus one-or-more clipboard-shape rectangle/polygon AND/OR/NOT/XOR through convex decomposition, selected-region and clipboard-region current-cell active-layer clip-cell creation, and undoable browser-search replacement for matching shape names, shape property keys/values, layer names, label/ruler text, cell names, cell property keys/values, instance names, instance property keys/values, and active-report DRC marker review notes/owners/signoffs/tags with document/current-cell/visible-hierarchy scope controls for layout objects, but not CAD-grade batch geometry derivation tools.

KLayout source: [Advanced Editing Operations](https://www.klayout.org/downloads/master/doc-qt5/manual/editor_advanced.html), [Geometry API](https://www.klayout.org/downloads/master/doc-qt5/programming/geometry_api.html)

### PCells And Libraries

- Parameterized cells with editable parameters.
- General PCell conversion from arbitrary guiding shapes beyond Glassworks's current bounds-based via-array guide conversion.
- General conversion from PCells to static normal cells.
- A library view and reusable layout libraries.

Glassworks has ordinary cells and instances plus a small via-array library macro with editable row, column, size, and pitch parameters that creates and places a reusable static child cell. Generated macro cells now carry stored cell properties describing the source macro and parameter values, selected macro cells can load their parameters back into the generator and regenerate their local geometry in place, and the cell browser has a Library filter for those generated cells. A selected current-cell rectangle, polygon, path, via, label, or measurement can also be converted into a placed via-array macro sized from the guide bounds, a generated macro cell can be detached into an ordinary static cell by clearing its macro metadata while preserving geometry and instances, and four options-backed via-array preset slots with deterministic JSON exchange form a tiny persistent reusable-macro catalog. This is still a narrow static-cell macro flow rather than general model-level PCells. Glassworks still does not have broader dynamic PCell regeneration, richer guide-to-parameter policies, general PCell-to-static conversion workflows, or a full reusable library system with external catalogs and multiple primitive types.

KLayout source: [PCell Operations](https://www.klayout.org/downloads/master/doc-qt5/manual/pcell_operations.html), [KLayout Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/basic.html)

### Full DRC Engine

- Scriptable DRC deck language integrated with the macro/script environment beyond Glassworks's current editable JSON DRC deck exchange.
- Hierarchical and region-constrained DRC operation beyond Glassworks's current full-layout run, current-view-cell root run, and selected current-cell rectangle/polygon region filter.
- Cell filtering and local checks beyond Glassworks's current active layout-cell root selection.
- Rich output to report databases or generated layout layers beyond Glassworks's current DRC report JSON and active-marker annotation-layer geometry output.
- Broader geometry predicates, edge-pair checks beyond Glassworks's current axis-aligned min-edge-spacing rule, derived layers beyond Glassworks's current bounds-based named `AND`/`OR`/`NOT` intersections/source unions/differences, and rule categories.

Glassworks has basic data-driven rules for min width, max width, min area, max area, bounds spacing, axis-aligned edge spacing, enclosure, forbidden overlap, and off-grid checks, deterministic editable JSON DRC deck export/import with bounds-based named `AND`/`OR`/`NOT` derived layers and derived min/max-width, min/max-area, min-spacing, min-edge-spacing, plus forbidden-overlap checks that overrides later full/cell/region runs and survives app-session save/load, explicit full-layout, current-cell, and selected current-cell rectangle/polygon-region DRC runs, plus active-marker annotation-layer output, but not a general KLayout-like DRC language or report ecosystem.

KLayout source: [DRC Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/drc_basic.html), [DRC Layer Reference](https://klayout.org/downloads/master/doc-qt4/about/drc_ref_layer.html)

### LVS And Device Extraction

- Layout-to-netlist extraction beyond Glassworks's current connectivity-component plus simple MOS-like device netlist JSON, SPICE-style `.subckt` export, and connectivity-only SPICE schematic comparison, including richer device-aware extracted circuits.
- Device extraction beyond Glassworks's current diffusion/poly crossing MOS-like devices with side-label source/drain/gate and overlapping well/body-label bulk inference, isolated labeled-poly resistor-like devices, and labeled metal-overlap capacitor-like devices, including true channel-split diffusion connectivity, richer well/substrate handling, broader resistor/capacitor extraction, and parameter extraction.
- Schematic netlist import and comparison beyond Glassworks's current SPICE `.subckt` comparison for named nets, supported MOS/resistor/capacitor device signatures, and exact MOS `L`/`W` dimension strings when supplied by the schematic, including hierarchy, broader devices, richer parameters, and property-aware matching.
- LVS report and cross-reference browsing beyond Glassworks's current SPICE compare summary and search-aware missing/extra net/device rows with extra-layout-net/device cross-probing.
- Hierarchy-aware LVS workflows and netlist browser integration.

Glassworks has full-layout flattened-geometry connectivity extraction that is independent of current display visibility, open/short reports, deterministic extracted-netlist JSON exchange with recent-file reload, searchable Connectivity inspector summaries with `key=value` selectors for health/count/source/trace/SPICE metadata, simple diffusion/poly crossing MOS-like device recognition with side-label source/drain/gate and overlapping well/body-label bulk inference, isolated labeled-poly resistor-like devices, and labeled metal-overlap capacitor-like devices carried through netlist JSON and SPICE export, a SPICE-style `.subckt` export with generated names for unlabeled components and comments for opens/shorts, and SPICE `.subckt` schematic comparison for named nets plus supported MOS/resistor/capacitor signatures, exact MOS `L`/`W` dimension strings, recent-file reload for the last schematic compare input, compact SPICE compare summary rows with issue-net counts in the net browser and inspector, separate missing/extra/net-level/device-level issue-net counts, mismatch compares that open the SPICE Issues net filter when there are browsable issue nets, selected-net device-terminal summaries, selected-net SPICE net/device status for extra layout devices and schematic-only missing-device references, net-browser SPICE Missing/SPICE Extra/SPICE Nets/SPICE Devices/SPICE Issues filters with live counts and matching status messages for missing-layout references, extra layout nets, net-level mismatches, device-level mismatches, and all browsable issue nets, filter-aware SPICE compare issue rows and select-first actions with visible row counts labeled separately from filter net counts, active category titles, no-match status text with active filter/search context, and cross-probeable rows prioritized in the missing and net filters, explicit missing-net issue status for schematic-only nets with no current layout target, net-browser search over SPICE/LVS net/device status, compact missing/extra device details, search-aware SPICE compare issue rows with extra-layout-net/device and missing-device-reference cross-probing plus direct first-matching issue selection in the net browser, and explicit clearing of the active SPICE compare report, but not true channel-split diffusion connectivity, rich substrate tap modeling, broader resistor/capacitor recognition, richer parameter/property-aware device comparison, extracted circuits, hierarchy-aware LVS, or full LVS-grade browsing.

KLayout source: [LVS Introduction](https://www.klayout.org/downloads/master/doc-qt5/manual/lvs_intro.html), [LVS Overview](https://www.klayout.org/downloads/master/doc-qt5/manual/lvs_overview.html), [LVS Device Extractors](https://www.klayout.org/downloads/master/doc-qt5/manual/lvs_device_extractors.html)

### Net Tracing Workflow

- Point-based single-net tracing beyond Glassworks's current basic Trace tool, selected-trace clearing control, one-shot trace-state clearing, and selected-net source-object/label cross-probe rows, including richer trace state controls.
- Trace-all-net workflows beyond Glassworks's current explicit Trace All action, extracted-component browser with labeled/unlabeled/device-connected/trace-history/shorted/open filters, selected-net source-object/label/device/issue cross-probe rows, and trace history seeding, including a full netlist browser.
- Trace path between two points beyond Glassworks's current route-point Trace Path connectivity check with multi-segment validation, failing-segment focus, direct connected-path net selection, path endpoint net selection, first-blocker endpoint net selection, coordinate- and selector-searchable route-point source/net rows with first-matching selection, per-point/all clearing, and compact point summaries, persistent pending endpoint markers outside the Route tool, per-segment connected/disconnected route-point path preview, length-, coordinate-, status-, and selector-searchable/focusable trace-path segment rows with named connected-segment and disconnected-endpoint net cross-probing, compact trace-path result/endpoint/segment summary/path-length/actionable first-blocker rows, including richer path-result visualization.
- Layer-stack editor for tracing configuration beyond Glassworks's current active built-in technology selector, searchable stack browser with `key=value` selectors, and per-link connectivity enable controls.
- Symbolic connectivity layers and boolean-derived connectivity layers.
- Save/load traced nets as L2N databases beyond Glassworks's current combined L2N database JSON exchange, searchable visible trace-state summary rows, and imported connectivity source metadata rows.
- Net tracing history beyond Glassworks's current searchable short trace history list and History net filter with component/history-position selector search, select-first and per-entry/all clearing, one-shot trace-state clearing, deterministic trace-state JSON round-trip with recent-file reload, selected/history/off trace-highlight mode, import-time History-filter activation, and selected/history/off trace-highlight controls.

Glassworks has full-layout flattened-geometry connectivity extraction that is independent of current display visibility, selected-net highlighting with a selected-trace clearing control, one-shot trace-state clearing, and selected-net source-object/label/device/device-peer/issue/open-peer cross-probe rows, a basic point Trace tool, a route-point Trace Path check with multi-segment validation and failing-segment focus, direct connected-path net selection, path endpoint net selection, first-blocker endpoint net selection, coordinate- and selector-searchable route-point source/net rows with first-matching selection, per-point/all clearing, and compact point summaries, persistent pending endpoint markers outside the Route tool, per-segment connected/disconnected route-point path preview, length-, coordinate-, status-, and selector-searchable/focusable trace-path segment rows with named connected-segment and disconnected-endpoint net cross-probing plus compact result, endpoint, segment summary, path-length, and actionable first-blocker rows, route-time same-net or same-component context for obstacle handling, an explicit Trace All action that seeds trace history and selects the largest component, selected/history/off trace-highlight modes, a searchable short trace history list and History net filter with component/history-position selector search, first-matching entry selection, and per-entry/all clearing, deterministic extracted-netlist JSON exchange with recent-file reload, SPICE-style netlist export, connectivity-only SPICE schematic comparison with recent-file reload and SPICE Issues filter activation for browsable mismatches, trace-state JSON export/import with recent-file reload, trace-highlight mode, and import-time History-filter activation, combined L2N database JSON export/import, searchable visible trace-state summary rows, imported connectivity source metadata rows, and a basic extracted-component net browser with aggregate netlist summary rows, searchable extracted-device and short/open issue cross-probe rows, first-matching global netlist item selection, labeled/unlabeled/device-connected/trace-history/SPICE-missing/SPICE-extra/SPICE-nets/SPICE-devices/SPICE-issues/shorted/open filters including nets connected through extra layout devices, search over device terminals, peer terminals, and short/open issue details, select-first matching net selection, component issue rows, device count rows, and compact selected-net device-terminal, issue, open-peer, and device-peer property summaries, but not KLayout's full trace-history/netlist-browser/L2N workflow.

KLayout source: [Net Tracing Feature](https://klayout.org/downloads/master/doc-qt4/manual/net_tracing.html), [DRC Netter Reference](https://www.klayout.org/downloads/master/doc-qt5/about/drc_ref_netter.html)

### Marker Browser And Report Databases

- Persistent multiple report databases per view beyond Glassworks's current bounded active-report list, app-session report-list restore, deterministic report-database JSON exchange, KLayout `.lyrdb` XML report database import/append/export subset with embedded marker-image payload preservation plus snapshot-gallery surfacing, append-loaded source-backed report databases, source-group selection/removal controls, and imported report-database source labels.
- Hierarchical marker categories beyond Glassworks's current fixed rule-family grouping, user-tagged category directory paths, and imported KLayout category-part directory paths with aggregate ancestor rows and descendant filtering.
- Marker directory and marker info panes beyond Glassworks's current rule-path plus user-tagged and imported KLayout category-part hierarchical directory rows and filter controls, top-level report-database summary/preview rows, selected-marker active-report context with aggregate stored-report summaries, listed marker IDs/messages, rule/category/directory, marker geometry/required/actual, source-object/source-cell/source-occurrence labels, source-layer/source-kind/source-bounds, review-state, signoff-audit, tag, snapshot, and report-diagnostic summaries, rule/state/review/snapshot/geometry/object/source-cell/source-object/report-warning layer-kind-bounds rows, source-shape/source-cell/source-occurrence cross-probe target-count summaries and controls, searchable active DRC report list with `key=value` selectors over report diagnostics and contained marker metadata, first-match selection, per-report delete controls, listed/total counts, and no-match context, and marker list with category filters, live filter counts, and active filter/category/directory/search row-count titles.
- Marker review workflows beyond Glassworks's current visited/important tags, hidden/waived state, preset notes, ownership presets, signoff presets with captured signer/note metadata, accumulated per-party role/revision/wall-clock-stamped signoff records, status-specific signoff filters, preset key/value tags, `key=value` marker review/tag selector search, and browser replacement for active-report notes/owners/signoffs/tags, including richer approval workflow semantics.
- Marker screenshot management and richer tagged-value editing UI beyond Glassworks's current selected-marker RGBA/PNG snapshot export, thumbnail-capable count/search-aware active-report snapshot gallery rows with searched-empty context and selectable marker cross-probe entries, selected and active-report snapshot metadata clearing, snapshot-tag filter, snapshot info rows, preset/custom key/value marker tags including category-tag grouping, selected-marker direct tag removal, active-report tag clearing, and browser replacement for active-report marker tags.
- Open/reload/save report databases beyond Glassworks's current deterministic active-report and bounded report-database DRC JSON exchange, first-class KLayout `.lyrdb` report database export/import/append actions, gzip-aware KLayout `.lyrdb` XML report database import/append/export plus single-file-ZIP-aware import subset with embedded marker-image payload preservation plus snapshot-gallery surfacing, report-database source metadata, replace-or-append report-database import, recent-file/reload integration, in-memory report selection, and active-marker annotation-layer geometry output.
- Import Calibre-style DRC/RVE marker formats beyond Glassworks's current text subset for inline and counted/uncounted multi-line polygon, rectangle, edge, point, and circle marker geometry with integer, decimal, scientific notation, DBU, um, nm, long-form metric unit aliases, separated numeric/unit tokens, order-independent keyed coordinate tokens including paired diagonal-corner, start/end endpoint, center/location, and vertex tuples, bracketed/underscore-indexed coordinate keys and tuples, keyed geometry-kind fields, labeled coordinate rows, pending keyed coordinate rows, plus keyed circle radius/diameter aliases, separated keyed fields, keyed origin-plus-size and center/centre-plus-size rectangles, numbered inline geometry rows, punctuated and whitespace-separated numbered multi-line polygon/rectangle/edge/point/circle coordinate rows, standard ASCII result-database top-cell precision headers, rule count/timestamp rows, `p`/`e` ordinal/count signatures, `CN` cell-space transforms, attached result-property lines, and colon-terminated/plural/short/attached geometry keyword aliases, native import status plus stored report-warning diagnostics for incomplete multi-line geometry including warning-only imports, accumulated free-form `@` and labeled message/description lines, labeled rule-check-name aliases with quoted rule-label normalization plus result/marker/violation record delimiters, labeled and combined required/actual values including separated `=`/`:` separators, plus lightweight quoted/unquoted `@ owner/note/signoff/approval/tag/tags` review metadata aliases including separated `=`/`:` separators, colon-separated tag items, and whitespace-separated tag items, direct severity/classification/source/run/tool/deck tag fields, and common non-`@` single- and multi-word `Cell`/`Cell Name`/`Layer`/`Layer Name`/`Source Cell`/`Source Layer`/`Datatype` context tags.
  The current text subset also covers tuple-valued rectangle `origin/size`, `center/size`, `min/max`, and `lower/upper` forms plus standard ASCII result-database polygon/edge result signatures; the remaining gap is full Calibre/RVE database compatibility rather than those text marker shapes.

Glassworks has an active-report DRC marker browser with a bounded report history for recent full/cell/region runs and imported marker reports that survives app-session save/load, fixed rule-family category grouping, rule-path plus user-tagged and imported KLayout category-part hierarchical category directory rows and directory filter controls with aggregate ancestor rows, descendant filtering, and search/listed-directory summaries, top-level report-database summary/preview rows, top-level listed marker IDs/messages, rule/category/directory, marker bounds/area/required/actual, source-object/source-object-label/cell/cell-label/layer/kind/bounds/occurrence/occurrence-label, listed review-state, listed signoff-audit, listed tag, listed snapshot, report-diagnostic, and cross-probe target summaries, selected-marker info/browser detail/snapshot/geometry/object/source-cell/source-object layer-kind-bounds rows with search/listed-marker context plus active-report database context, aggregate stored-report summaries, imported report-database source labels, source-group selection/removal controls and inspector source summaries for imported/appended report databases, selected-marker matching-report/source summaries across loaded report databases, report warning/error diagnostics that can coexist with imported marker rows or persist as warning-only imported reports, and cross-probe target-count summaries in both selected-marker info and browser detail rows, selected-marker source-shape/source-cell/source-occurrence cross-probe controls, searchable active DRC report list with `key=value` selectors including report database source, contained marker rule/source/category/directory/tag/signoff metadata, diagnostic, warning, error, and diagnostic-message/severity searches, first-match selection, per-report delete controls, listed/total counts, and no-match context, active/selected-shape/active-layer/hidden/waived/visited/important/noted/owned/signed-off/needs-review/accepted/rejected/tagged/snapshot filters with live counts, active filter/category/directory/search row-count titles and no-match context, `key=value` selector search for marker rules/state/source cells/source objects/source layers/source kinds/source bounds/custom category/geometry/review/signoff metadata/tags/per-party role/revision/wall-clock-stamped signoff records, ID/rule/state/category/directory/source-cell/source-object/source-layer/source-kind/source-bounds/signoff/signoff-role/signoff-stamp/size/area/required/actual sort controls, click-to-focus and first-matching marker selection, selected-shape focus, automatic visited tagging, important tagging, preset review notes, owner presets, signoff presets that capture current owner/note as signer/rationale metadata and accumulate per-party role/revision/wall-clock-stamped signoff records with selected-marker info/browser detail summaries, preset and custom key/value tags including category tags with selected-marker direct tag removal, active-report tag clearing, and browser replacement for active-report notes/owners/signoffs/tags/signoff records, selected-marker RGBA/PNG snapshot export plus thumbnail-capable count/search-aware active-report snapshot gallery rows, selectable snapshot marker cross-probing, and selected and active-report snapshot-metadata clearing with screenshot path/bounds/size/format tags carried by report JSON, deterministic active-report and bounded report-database DRC JSON export/import plus append-loaded source-backed report databases, KLayout `.lyrdb` XML report database import/append for common `box`/`edge`/`edge-pair`/`point`/`polygon`/`path`/`label`/geometric `text` marker values plus text-only diagnostic items and item category/cell/tags/tag-description/comment/visited/multiplicity metadata, imported original tag identifiers such as hyphenated tag names through KLayout-quoted item tag lists, quoted category path parts with `/`, `.`, or hyphens inside category names, declared category-description, declared tag-description, and declared cell metadata preservation/re-export, imported root description/top-cell/original-file/generator metadata, cell layout-name/reference metadata including KLayout-native empty `<layout-name/>` plus `<ref>` nodes and legacy `<reference>` nodes, multiple/scaled/arbitrary-angle cell-reference transform paths represented as separate imported marker rows, current-layout hierarchy fallback when the RDB cell graph is incomplete, unique unqualified declared cell-variant fallback, supported tagged geometry value preservation/re-export for the imported `box`/`edge`/`edge-pair`/`point`/`polygon`/`path`/`label`/geometric `text` payloads, tagged item-reference value preservation/re-export, text/string/float/tagged scalar value preservation/re-export including quoted leading/trailing, empty, and whitespace-only text payloads, ordered item-value sequence preservation/re-export, and tagged raw preservation/re-export of unsupported item values attached to imported markers, KLayout `.lyrdb` XML report database export to `.lyrdb` and gzip-compressed `.lyrdb.gz` paths for marker bounds, messages, category/cell metadata including the empty-name All cells declaration for cell-less diagnostic items, original imported tag identifiers and quoted category names that contain `/` or `.`, root description/top-cell/original-file/generator metadata, category-description metadata while leaving generated no-description categories as native empty `<description/>` elements with KLayout-style leaf `<categories>` containers, tag-description metadata, declared cell metadata, visited/hidden/important/waived/custom tag state including `#hidden`/`#hide` item-tag import and native empty item `<tags/>` output, item multiplicity, comments, diagnostic text items, cell layout-name/reference metadata with native empty `<layout-name/>`, expanded empty `<references>` containers, and `<ref>` output, supported tagged geometry value payloads, ordered item-value sequences, tagged item-reference values, text/string/float/tagged scalar values with native unquoted simple text payloads and quoted leading/trailing, empty, and whitespace-only text payloads, tagged raw item values, preserved raw item values, and embedded marker-image payloads from imported RDB images or marker snapshot files, Calibre/RVE-style text marker import for inline and counted/uncounted multi-line polygon/rectangle/edge/point/circle geometry with integer, decimal, scientific notation, DBU, um, nm, long-form metric unit aliases, separated numeric/unit tokens, separated keyed fields, order-independent keyed-coordinate tokens including paired diagonal-corner, start/end endpoint, center/location, and vertex tuples, bracketed/underscore-indexed coordinate keys and tuples, keyed geometry-kind fields, labeled coordinate rows, pending keyed coordinate rows, plus keyed circle radius/diameter aliases, keyed origin-plus-size and center/centre-plus-size rectangles, numbered inline geometry rows, punctuated and whitespace-separated numbered multi-line polygon/rectangle/edge/point/circle coordinate rows, standard ASCII result-database top-cell precision headers, rule count/timestamp rows, `p`/`e` ordinal/count signatures, `CN` cell-space transforms, attached result-property lines, and colon-terminated/plural/short/attached geometry keyword aliases, native import status diagnostics plus stored report warnings for incomplete multi-line geometry, accumulated free-form `@` and labeled message/description lines, labeled rule-check-name aliases with quoted rule-label normalization plus result/marker/violation record delimiters, labeled and combined required/actual values including separated `=`/`:` separators, plus lightweight quoted/unquoted `@ owner/note/signoff/approval/signoff_role/signoff_at/tag/tags` review metadata aliases including separated `=`/`:` separators, colon-separated tag items, and whitespace-separated tag items, non-`@` single- and multi-word cell/source-cell/layer/source-layer/datatype context tags, direct severity/classification/source/run/tool/deck tag fields, and per-party signoff-record seeding, active-marker annotation-layer geometry output, and persisted hide/waive/visit/important/note/owner/signoff/signer/signoff-note/signoff-records/tags state for stable DRC marker keys with selected-marker and active-report bulk clearing. It still does not have KLayout's generic persistent multi-RDB marker browser, full `.lyrdb` reload/reference/value typed compatibility beyond the currently preserved ordered category/tag/geometry/tagged-geometry/reference/tagged-reference/scalar/tagged-scalar/raw/tagged-raw subset, full persistent user-defined hierarchical category trees beyond category-tag directory grouping, full marker-info/cross-probe panes, full Calibre/RVE database compatibility, or richer approval workflow semantics.
Imported embedded RDB images are now surfaced in marker snapshot labels, filters, summaries, and gallery rows, with canvas-backed thumbnail rendering in the native window and snapshot-render paths for embedded `.lyrdb` PNG payloads plus PNG/RGBA marker snapshot files. Unsupported `.lyrdb` item values attached to markers are preserved and re-exported as KLayout-compatible text-encoded raw payloads, imported root description/top-cell/original-file/generator metadata, marker-cell layout-name plus parent/transform cell-reference metadata, declared-cell name/variant/layout-name/reference metadata including literal colon-containing cell names distinct from KLayout variants even when they share the same displayed `name:variant` text, original tag identifiers in declarations, KLayout-native `#tag`/`#'tag name'` item tag lists, and KLayout-native `[#tag]`/`[#'tag name']` value-tag prefixes, quoted and backslash-escaped category path parts, supported tagged marker geometry value payloads, ordered item-value sequences, tagged item-reference metadata through KLayout-compatible text payloads, text/string/float/tagged scalar values including native unquoted simple text payloads plus quoted leading/trailing, empty, and whitespace-only text payloads, text-only diagnostic items, geometric label/text/DText values, and declared-category/declared-tag descriptions are retained on re-export. Point, edge-pair, polygon-with-hole bounds, path begin/end-extension values, and geometric text anchors are interpreted as marker geometry, and resolvable RDB cell-reference transforms, including multiple representative paths, scaling, arbitrary-angle rotation/mirroring, current-layout hierarchy fallback, and unique unqualified declared cell-variant fallback, are imported as separate marker rows in the RDB top-cell context. The remaining embedded-image/reference/value gap is typed interpretation of the complete KLayout RDB value/reference model beyond the supported and preserved ordered category/tag/box/edge/edge-pair/point/polygon/path/label/text/string/float/tagged-geometry/tagged-reference/tagged-scalar/tagged-raw subset.
Legacy `string:` scalar values are accepted on import for existing Glassworks-authored RDB files, but `.lyrdb` export now emits those scalar strings as KLayout-compatible `text:` values because KLayout's own `RdbItemValue` rejects `string:` values.
Legacy geometric `text: ('...',trans)` values are accepted on import but normalized to KLayout-compatible `label: ('...',trans)` on `.lyrdb` export. Legacy `rect:`/`rectangle:`, `edge_pair:`, and `point:` geometry values are also accepted on import and exported as KLayout-compatible `box:`, `edge-pair:`, and zero-size `box:` values respectively.
Legacy `reference:` item values are accepted on import, but `.lyrdb` export now emits them as KLayout-compatible `text: 'reference: ...'` values and restores item-reference metadata on re-import.
Unsupported raw item values are accepted on import, but `.lyrdb` export now emits them as KLayout-compatible `text: 'glassworks-raw-value: ...'` values and restores raw metadata on re-import.
Value-level tags are exported with KLayout's native `[#tag]`/`[#'tag name']` prefixes while the importer accepts both native hash-prefixed and earlier bare quoted value-tag prefixes.
Item tag lists are exported with KLayout's native `#tag`/`#'tag name'` syntax while the importer accepts both native hash-prefixed and earlier bare quoted tag items.
For Calibre/RVE text import specifically, tuple-valued rectangle `origin/size`, `center/size`, `min/max`, and `lower/upper` forms plus standard ASCII polygon/edge result signatures are part of the current subset.

KLayout source: [Marker Browser](https://www.klayout.org/downloads/master/doc-qt5/manual/marker_browser.html)

### Layer Display And Layer Set Management

- Used/unused layer workflows beyond Glassworks's current layer-panel usage counts, row markers, All/Used/Empty/Visible/Hidden row filters with live counts, active-layer isolation, invert visibility, inactive-empty-layer pruning with live removable-layer counts, hierarchy-aware layer deletion, and basic visibility presets with live affected-layer counts.
- Layer animations, bitmap/custom stipple patterns, and richer display styles beyond Glassworks's current persisted/rendered outline, hatched, cross-hatched, and built-in normal/dense/sparse stippled fill styles plus solid/dashed/dotted/dash-dot line styles.
- User-defined or technology-imported hierarchical layer trees beyond Glassworks's current fixed two-level All/FEOL/Routing/Text layer-panel group tree with FEOL and routing child filters, live row counts, and searchable inspector directory summaries with `key=value` selectors for group/parent/current/count metadata.
- Multiple layer setups in tabs beyond Glassworks's current eight named tab-selectable layer setup slots with compact saved/empty tab markers, searchable inspector summaries with `key=value` selectors for slot/name/state/visibility/group/usage/depth metadata, visible-layer counts, and per-slot/all clear controls.
- Load/save layer sets beyond Glassworks's current deterministic JSON exchange for eight searchable layer setup slots that preserve setup names, visibility, fixed hierarchical layer group filter, layer-row usage filter, and per-layer hierarchy depth overrides, with per-slot/all clearing.
- Richer hierarchy-depth setup workflows beyond Glassworks's current active-layer and current-layer-group options-backed depth override controls, including arbitrary saved group policies and bulk editing across imported group trees.
- Property selectors and view transformations beyond Glassworks's current property-bearing shape/cell/instance quick filters and browser-search property selectors for shape names, shape nets, and stored shape/cell/instance key/value metadata.

Glassworks has layer colors, visibility, order, purpose, technology-defined layers, used/unused usage markers, basic usage-driven visibility presets with live affected-layer counts plus active-layer isolation, invert visibility, undoable inactive-empty-layer pruning with live removable-layer counts, and hierarchy-aware layer deletion that restores matching top-level and cell-local shapes on undo, options-backed fixed hierarchical layer filters for All/FEOL/Routing/Text groups and FEOL/Routing child groups with live row counts, searchable layer group tree summaries with `key=value` selectors for group/parent/current/count metadata, and All/Used/Empty/Visible/Hidden rows with live counts, persisted rendered fill/line display style controls with outline, hatched, cross-hatched, normal/dense/sparse stippled, dashed, dotted, and dash-dot 2D geometry effects, active-layer and current-layer-group hierarchy depth overrides persisted in options, eight named options-backed layer setup tabs with compact saved/empty tab markers and searchable inspector summaries with `key=value` selectors for slot/name/state/visibility/group/usage/depth metadata plus saved visible-layer counts that save visibility, layer group, row usage filter, and per-layer depth overrides with per-slot/all clearing, and deterministic JSON exchange for those slots, but not the full display/layer-set management model.

KLayout source: [KLayout Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/basic.html)

### Browsers And Inspectors

- Shape browser beyond the current Glassworks current-view visible-occurrence browser with shared text search, configurable Summary/Geometry/Relations/All detail presets, quick active-layer/selected-shape/selected-net/property-bearing/shape-kind filters with live counts, active filter/search row-count titles, `key=value` selector search for source cells, layers, kinds, names/nets, label text, and stored shape properties, select-first matching occurrence action with no-match context, ID/layer/cell/kind/area sort controls, and property rows including stored shape key/value metadata plus kind-specific geometry rows for rectangle/polygon area, path width/length, via stack/size, and label text/position: cross-layout browsing and richer multi-layout object views.
- Instance browser beyond the current Glassworks current-cell and hierarchy-wide child-instance browser scopes with shared text search, configurable detail presets, named/property-bearing/array/hidden-target/visible-target/leaf-target/branch-target/identity/transformed quick filters with live counts, active scope/filter/search row-count titles, `key=value` selector search for parent/target cells, names, transforms, arrays, target roles/counts/bounds, and stored instance properties, select-first matching instance action with no-match context, sort controls, and property rows including stored instance key/value metadata plus target-cell role/count/bounds rows: richer filters and cross-layout browsing.
- Cell browser beyond the current flat all-cell list with shared text search, configurable detail presets, current/used/unused/library/property-bearing/empty/leaf/branch/parent/child/sibling quick filters with live counts, active filter/search row-count titles, hierarchy-depth and parent/child/sibling/ancestor/descendant relation rows, hierarchy-depth sorting, `key=value` selector search for cell ids/names, usage/tree roles, hierarchy depth, relation cells, counts, bounds, and stored cell properties, select-first matching cell action with no-match context, and selector-searchable collapsible hierarchy tree with row-count titles and search-revealed descendant paths: richer hierarchy display modes and cross-layout browsing.
- Rich object property browsing across loaded layouts.
- Marker browser integration beyond the current DRC marker browser, including generic report databases and richer cell/object cross-probing beyond selected-marker source-object controls.

Glassworks has selected-shape and selected-instance details plus standalone current-view shape, current-cell or hierarchy-wide instance, cell, DRC marker, and extracted-net browsers in the 2D inspector with shared text search, searchable top-level `Cells` inspector summaries, configurable browser detail presets, basic quick filters including selected, property-bearing, and shape-kind shapes with live counts, active filter/search row-count titles, `key=value` selector search for source cells/layers/kinds/name/net/label text/stored shape properties plus select-first matching shape selection with no-match context, named/property-bearing/array/hidden-target/visible-target/leaf-target/branch-target/identity/transformed instance views with live counts, active scope/filter/search row-count titles, `key=value` selector search for parent/target cells/name/transform/array/target metadata/stored instance properties plus select-first matching instance selection with no-match context, current/used/unused/hidden/visible/library/property-bearing/empty/leaf/branch/parent/child/sibling/ancestor/descendant cell views with live counts, active filter/search row-count titles, `key=value` selector search for cell identity/hierarchy/relation/count/bounds/property metadata plus select-first matching cell viewing with no-match context, DRC marker views with live filter counts, active filter/category/directory/search row-count titles, `key=value` marker metadata selector search, first-matching marker selection with no-match context, selected-shape filtering, selected-marker source-object cross-probing, and ID/rule/state/category/directory/size/area/required/actual sorts, and labeled/unlabeled/device-connected/trace-history/SPICE-missing/SPICE-extra/SPICE-nets/SPICE-devices/SPICE-issues/shorted/open net views with `key=value` component selector search for ids/names/counts/bounds/issues/devices, select-first matching net selection, sort controls, and property rows including compact selected-net device, issue, open-peer, and device-peer summaries plus stored shape, instance, and cell metadata key/value rows, instance target-cell object rows, and kind-specific shape geometry rows, but not KLayout's richer cross-layout browser suite with advanced object views.

KLayout source: [KLayout Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/basic.html)

### Measurement, Images, And View Utilities

- Ruler workflows beyond Glassworks's current direct/horizontal/vertical/Manhattan measurement annotations, searchable measurement browser with `key=value` selector search for source cells, layers, modes, endpoints, deltas, lengths, angles, and bounds, selected-ruler, active-layer and mode filters with live counts, active filter/search row-count titles and no-match selection status context, ID/length/angle/mode/layer/source-cell sort controls, listed-ruler aggregate summaries, mode/endpoint/delta/length/angle/source-cell property rows, first-matching ruler selection, selected-ruler focus/deletion, and undoable visible-ruler cleanup.
- Add images into the view beyond Glassworks's persisted reference-image overlays, deterministic reference-image JSON exchange, global/per-image/bulk visibility toggles, per-image focus/opacity/order/removal controls, searchable source/pixel-size/visibility/opacity/bounds/landmark inspector rows with `key=value` selectors, bulk reference-image clearing, and translucent image/placeholder overlay rendering.
- Landmarks to align images beyond Glassworks's current two-or-more-landmark axis-aligned reference-image alignment plus bulk/per-image align, seed, clear, and selected-geometry fit controls.
- Bookmarked views and previous-view navigation beyond Glassworks's current eight-slot named/options-backed layout view bookmarks, searchable visible current/previous/saved-slot inspector summaries with `key=value` selectors for slot/name/state/top cell/zoom/pan/hierarchy metadata, deterministic bookmark JSON exchange, origin/bounds/selection focus, per-slot/all clear actions, and previous-view toggle.
- Full session save/restore beyond Glassworks's current deterministic app-session JSON that bundles workspace data, app options, active shell/view state, current 2D layout view, and recent exchange-path list, including richer multi-window session chrome.
- Screenshot export workflows beyond Glassworks's current File-menu deterministic PNG, RGBA, and portable PPM snapshot export.

Glassworks has measurement annotations with direct/horizontal/vertical/Manhattan drawing modes, a searchable measurement browser with `key=value` selector search for source cells, layers, modes, endpoints, deltas, lengths, angles, and bounds, selected-ruler, active-layer and mode filters with live counts, active filter/search row-count titles and no-match selection status context, ID/length/angle/mode/layer/source-cell sort controls, listed-ruler total-length and mode summaries, mode/endpoint/delta/length/angle/source-cell property rows, first-matching ruler selection, selected-ruler focus/deletion, and undoable visible-ruler cleanup, persisted reference-image overlays with global/per-image/bulk visibility controls, per-image focus/opacity/order/removal controls, searchable source/pixel-size/visibility/opacity/bounds/landmark inspector rows with `key=value` selectors, bulk reference-image clearing, landmark-based axis-aligned alignment, bulk/per-image landmark align/seed/fit/clear controls, snapshot tooling, File-menu deterministic PNG, RGBA, and portable PPM screenshot export, File-menu app-session/layout/workspace JSON save/load, File-menu JSON/GDS/CIF/DXF/DEF/LEF hierarchy-preserving root merge, File-menu JSON/GDS/CIF/DXF/DEF/LEF import as child-cell, extra-top-cell, and flattened-merge workflows, File-menu recent entries and reload-most-recent for native session/layout/workspace/GDS/CIF/DXF/DEF/LEF/reference-image exchange paths, Origin/Layout Bounds/Selection focus actions, and eight named/options-backed layout view bookmark slots with searchable visible current/previous/saved-slot inspector summaries, deterministic JSON exchange, per-slot/all clear actions, and previous-view navigation, but not KLayout's full interactive ruler/image/session/view workflow.

KLayout source: [KLayout Basics](https://www.klayout.org/downloads/master/doc-qt5/manual/basic.html)

### Automation, Macros, And Plugins

- Ruby/Python scripting against the layout database.
- Macro Development IDE.
- Scriptable DRC/LVS/net extraction workflows.
- Plugin factories, toolbar integration, and custom UI extensions.
- Broader package/plugin ecosystem.

Glassworks has Rust internals and command-like UI actions, but not a user-facing scripting or plugin system.

KLayout source: [KLayout User Manual](https://www.klayout.org/downloads/master/doc-qt5/manual/index.html), [Application API](https://www.klayout.org/downloads/master/doc-qt4/programming/application_api.html)

## Lower-Priority Or Specialized Gaps

- Manufacturing/EDA-specific property propagation and richer property-aware selection beyond Glassworks's current browser property-selector search and select-first actions.
- Rich text handling and text-object options beyond Glassworks's current basic label geometry and direct Label tool.
- Advanced import/export compatibility controls for GDS variants.
- Gerber import preparation workflows.
- Multi-layout comparison workflows beyond Glassworks's current layout diff surface.
- Technology manager UI for editable libraries, layer properties, macros, net tracer settings, and connectivity stacks beyond Glassworks's current active built-in technology selector, searchable stack browser with `key=value` selectors, and per-link connectivity enable controls.
- External marker/database interchange for signoff review beyond Glassworks's current Calibre/RVE-style text marker subset with lightweight `@ owner/note/signoff/approval/tag/tags` metadata plus direct review tag fields.

## Suggested Roadmap Order

1. OASIS plus stronger GDS reader/writer options.
2. Complete hierarchy navigation: richer cell-management workflows.
3. Layer/shapewise boolean and sizing operations.
4. KLayout-style marker/report database import/export.
5. Point net tracing and full trace-all-net browser.
6. Scriptable DRC deck language or a deliberately smaller dataflow rule system.
7. Deeper dynamic PCells or persistent reusable libraries.
8. LVS/device extraction only after connectivity, derived layers, and report browsing are stronger.

## Notes

- Some KLayout features overlap with Glassworks concepts but are still listed because Glassworks's implementation is narrower. For example, Glassworks has DRC, but not scriptable DRC; has a flat hierarchy cell browser with current/used/unused/hidden/visible/library/property-bearing/empty/leaf/branch/parent/child/sibling/ancestor/descendant filters, searchable collapsible hierarchy tree, parent/child context actions, selected-instance descend, view-only cell hide/show, box-only hierarchy view mode, numeric min/max hierarchy controls, shallow selected-instance variants, standalone current-cell duplication, and current-view shape/instance browsers, but not deeper KLayout-style variant policies; has a parameterized via-array library macro with stored generation metadata, selected guide-shape conversion, detach-to-static metadata clearing, and four options-backed preset slots with deterministic catalog exchange, but not dynamic editable PCells or full persistent layout libraries; has full-layout flattened-geometry connectivity, simple MOS-like, resistor-like, and capacitor-like device recognition, a basic point Trace tool, direct net-label placement, selected/history/off trace highlighting, a searchable and clearable short trace history list, deterministic extracted-netlist and trace-state JSON exchange, SPICE export and supported MOS/resistor/capacitor-signature comparison, and a basic extracted-component net browser with labeled/unlabeled/device-connected/trace-history/SPICE-extra/shorted/open filters, but not full L2N browsing or full device-aware LVS; has measurement annotation property rows, selected-ruler focus and visible-ruler cleanup, persisted reference-image overlays with visibility, focus, opacity, order, removal, and landmark alignment, deterministic app-session/layout/workspace JSON save/load/reload-most-recent, JSON/GDS/CIF/DXF/DEF/LEF hierarchy-preserving root merge, JSON/GDS/CIF/DXF/DEF/LEF import as child-cell, extra-top-cell, and flattened-merge workflows, undoable browser replacement for layout-object text/properties plus active-report DRC marker review metadata, options-backed recent exchange paths, and eight named/options-backed layout view bookmark slots with deterministic JSON exchange, origin/bounds/selection focus, clear actions, and previous-view toggle, but not full multi-view session restore.
- KLayout is a mature general layout viewer/editor. Glassworks's strongest differentiators are native/WASM Rust architecture, collaboration, fab-domain workflow modeling, and custom 3D/process views, which are outside this specific 2D gap list.
